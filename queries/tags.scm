; Code navigation tags for SyMon.
;
; These captures identify named top-level constructs for editor navigation.

(signature
  (identifier) @name) @definition.interface

(def_expr
  (identifier) @name) @definition.function
